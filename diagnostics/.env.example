# System Diagnostics Agent — Band URLs only (no Featherless key).
# Copy to .env in this folder, or rely on the repo-root .env (recommended).
THENVOI_REST_URL=https://app.band.ai/
THENVOI_WS_URL=wss://app.band.ai/api/v1/socket/websocket
